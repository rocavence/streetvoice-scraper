# Cloudflare Workers 部署設定
# cp .env.example .env  然後填入你的資訊
#
# API Token → https://dash.cloudflare.com/profile/api-tokens
#             選擇「Edit Cloudflare Workers」模板，依需求加上 R2 權限
# Account ID → https://dash.cloudflare.com → Workers & Pages → 右側欄位

CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
