{
  "name": "streetvoice-scraper",
  "version": "2.0.0",
  "private": true,
  "description": "StreetVoice scraper — Cloudflare Workers edition",
  "scripts": {
    "setup": "cp -n .env.example .env && echo 'Created .env' || echo '.env already exists'",
    "check": "wrangler whoami",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "wrangler": "^4"
  },
  "keywords": ["streetvoice", "scraper", "cloudflare-workers"],
  "license": "MIT"
}
